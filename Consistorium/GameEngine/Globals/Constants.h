namespace Globals
{
	const float PIXELS_PER_METER = 50;
	const float DEFAULT_PLAYER_WIDTH = 25;
	const float DEFAULT_PLAYER_HEIGHT = 80;
	const float BLOCK_WIDTH = 31;
	const float BLOCK_HEIGHT = 31;
}