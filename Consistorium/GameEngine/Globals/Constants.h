namespace Globals
{
	const float PIXELS_PER_METER = 25;
	const float DEFAULT_PLAYER_WIDTH = 64;
	const float DEFAULT_PLAYER_HEIGHT = 80;
	const float BLOCK_WIDTH = 60;
	const float BLOCK_HEIGHT = 60;
}